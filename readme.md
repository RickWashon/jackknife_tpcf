# jackknife_tpcf

This repository provides a lightweight setup for jackknife sampling error estimates based on pair counts computed with `Corrfunc`.

## Requirements

Install Python dependencies listed in `requirement.txt`:

```bash
pip install -r requirement.txt
```
